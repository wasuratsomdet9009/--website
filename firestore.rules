rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Production Readiness: Deny all direct client access. 
      // All database operations must go through the Backend API (Cloud Functions)
      // which uses the Admin SDK (bypassing these rules).
      allow read, write: if false;
    }
  }
}
